#include "archivo.h"
#include <string.h>

int main(int argc, char *argv[]){

    // char nombreArchivo[100];
    // char buffer[100];
    
    // printf("Ingrese el nombre del archivo: \n");
    // scanf("%s", &nombreArchivo);

    // memcpy(buffer, nombreArchivo+7, strlen(nombreArchivo));

    // printf("buffer: %s \n", buffer);
    // int cantidad_carga = atoi(strtok(buffer,  "_"));
    // int peso_max = atoi(strtok(NULL, buffer));

    // printf("%d %d \n", cantidad_carga, peso_max);

    // printf("%s \n", nombreArchivo);



    struct archivo *archivo = archivo_crear();



 
    
 
    



    return 0;
    
    //struct archivo *archivo = archivo_crear(nombre_archivo);


}